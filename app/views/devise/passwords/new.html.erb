<div class="min-h-[70vh] flex items-center justify-center">
  <div class="w-full max-w-md bg-white shadow-lg rounded-2xl p-8 border">

    <h1 class="text-2xl font-bold mb-2 text-gray-900">
      WineHub
    </h1>

    <p class="text-gray-600 mb-6">
      Zaboravljena lozinka? Upiši email i poslat ćemo ti link za reset.
    </p>

    <%= form_for(resource,
                 as: resource_name,
                 url: password_path(resource_name),
                 html: { method: :post, class: "space-y-5" }) do |f| %>

      <%= render "devise/shared/error_messages", resource: resource %>

      <div>
        <%= f.label :email, "Email", class: "block text-sm font-semibold mb-1" %>
        <%= f.email_field :email,
                          autofocus: true,
                          autocomplete: "email",
                          class: "w-full border rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" %>
      </div>

      <div>
        <%= f.submit "Pošalji upute za reset lozinke",
                     class: "w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 rounded-lg transition shadow" %>
      </div>

    <% end %>

    <div class="mt-6 text-sm text-gray-500 text-center">
      <%= render "devise/shared/links" %>
    </div>

  </div>
</div>