<h1 class="text-2xl font-bold mb-4">Katalog proizvoda</h1>

<div class="grid gap-4">
  <% @products.each do |product| %>
    <div class="border rounded p-4">
      <div class="font-semibold"><%= link_to product.name, product_path(product) %></div>
      <div class="text-sm text-gray-600">SKU: <%= product.sku %></div>
      <div class="mt-2"><%= number_to_currency(product.price_cents / 100.0, unit: "€") %></div>
    </div>
  <% end %>
</div>