<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Wholesale Orders" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Wholesale Orders">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <nav class="bg-gray-800 text-white px-6 py-4 flex gap-4">
    <%= link_to "Home", root_path, class: "hover:underline" %>
    <%= link_to "Katalog", products_path, class: "hover:underline" %>

    <% if user_signed_in? %>
      <%= link_to "Profil", profile_path, class: "hover:underline" %>

      <% if current_user.admin? %>
        <%= link_to "Admin", admin_root_path, class: "hover:underline" %>
      <% end %>

      <%= button_to "Odjava", destroy_user_session_path, method: :delete, class: "hover:underline" %>
    <% else %>
      <%= link_to "Prijava", new_user_session_path, class: "hover:underline" %>
      <%= link_to "Registracija", new_user_registration_path, class: "hover:underline" %>
    <% end %>
  </nav>

  <main class="container mx-auto mt-10 px-5 flex">
    <%= yield %>
  </main>
  </body>
</html>
